


 [-] не распознаётся метод getState в thunk-функции см. ./actions.js

    
 [-]  fetch ---> axios :
        Куча ошибок при попытке использования (уже добавлял в package.json "type" : "module")
            0 при добавлении в package.json "type" : "module" 
                - module not found: Error: Can't resolve './reducers'
                то же с ./components/app
            1 не тот формат аргументов
            2 говорит, мол сервер не поддерживает заголовок "grant_type" (он есть в доке) 

 [+-] style and components:
        0 почему-то не подцепились стили в компоненте StyledCard из contract-card.js
        1 покрасивее в коде оформить "волны" на заднем плане
          (сейчас это здоровенный <svg> с множеством <path> внутри, которые не понятно, как в отдельный компонент обернуть) 
        2 задний фон произвольного размера


  [?] есть ли какие-то советы по методам изучения материала, гуглежу 

  [?] в любом компоненте для обёртки использую div - это нормально или стоит другие тэги использовать

  [?] рэндеринг в React:
        - что о нём нужно знать сейчас(и где это можно найти)

  [?] props.Children:
        - хочется услышать что-нибудь о них(зачем они нужны), 
          т.к. переодически вижу, что их применяют в кодовых примерах 





