   работа с git: https://habr.com/ru/post/588801/
   git pull - обновить локальное хранилище
   детально про reset HEAD: https://russianblogs.com/article/77221618422/
   ключ: gitKey.txt

  --------------------------------------------------------------------------------------------------
Фронт:
   0) уже есть универсальная функция, которая может запрашивать
      любое свойство деталей контракта - getContractsInfo(apiKey, id, detail)
         

   1) Вёрстка деталей контракта:

      * подготовил стейт:
         - помни, что в стейт кладём только values из table = {anot, values}, 
            anot - пишем сами для всех страниц
      
      * anot - написать вручную для всех страниц

      * останется развесить 

   2) хлебные крошки - см п.0 

   3) поиск в шапку, а сам поиск скорее всего благодаря запросу на сервер 



TODO:


-About svg problem:
 clerc solution: store each svg as <svg> tag - made React-component:
   
   const someSvg = (color) =>{
      return <svg>
                  fill={color}
                  some svg magic 
             <svg/>
   }


TODO - make fixed head, footer and body(contains page data)

!!!
 WANTED - CRUD - add/modify/del data => modal architecture: 

------------------------------------------------------------------------
  Modal architecture: see galerey
   Modal-window - blocks everything outside itself 
  
   +++ Window - done
   >>> CRUD: - fuck, it(add new) works!!!
   !!! ISPRAVIT 167 line in login page - /contracts --> /test 
         state:
            modalBuffer = {}

         steps of every modal:
            +++ m_push_buffer()      // some changes/ new data
            +++ m_vipe_buffer()      // free buffer after use   
      >>>   --- refresh_page()       // send query to server again about contracts/characteristics/... 

   TODO: 
    - !!! we use same root "modal_smth_method" to create/modify
            4 ex: Modal_contract - root 4 Modal_add_contract(), Modal_modify_contract
        
    - validate input in needed poles so that serv would "eat" query 
    - researching how to modify specific pole(s) 

   

------------------------------------------------------------------------
 Solve State holding:
   just use some hook that catches refreshing and give to it whole state 
   - mb not effective or secure - so watch solving on a net


------------------------------------------------------------------------



- Global styles are realy useful & easy to use/manage:
   colors (blue and hardBlue)
   backgrounds
   fonts(AnotFont, TblFont, ...)


   [!!!] ResponsiveTable - used to add scroll-bar near large table
         - meant to be used in each page(contracts, contract details, ...)


 [?!] почему-то useApi(), использующее  AxiosInstance baseurl = http://server.clerc.ru/api/v2.0
      ведёт себя по разному: 
      для getContracts - baseurl "сам" подставляется в url
      getContractsInfo - нужно baseurl самому приписывать

 [*] WakaTime - плагин, дающий стату по разработке

 [!!] Нужно перенести все диспатчи страниц, прямо в функцию, делающую запрос:
         а то, когда нужно будет детали контракта сохранять - очень много лишнего кода выйдет

 [!?] POST + SQL:
        помогло исправить тело запроса на закодированное: JSON.stringify(requestOptions) 
        но почему это здесь необходимо???

 [!!] Нужно хранить в стейте результаты
       ответов по вкладкам подробностей о контракте, 
       например:  state.
                     app_reducer.
                        contractDetails.charecteristics
       
 [!!] cделать отображение нужной таблицы в зависимости от текущего урла на contract-dtails-page
      

 [??] как одинаковый размер сделать карточкам:
       {flex: 1 1 0;
       width: 0;}

 

 [?!] Checking logined state: toggle_logined --> is_loggined
      Temporary measure: TODO: make some function to check if token is valid
       => if it`s true: toggle loggined state  (with rights of authenticated user)
      
      toggle_logined:(state, action)  =>   {state.sessionData.isLoginned = ! state.sessionData.isLoginned}

 [?] Как-то сохранять данные после обновления страницы

 [?] Redirect: /login      -->   /contracts
               /           -->   /login или /contracts
               /contracts  -->   /login - если токен неверен

   - если произошёл логин <=> {
      
      удачный пост запрос(для /login),
      
      состояние залогиненности - проблема, тк токены имеют время жизни
      Правильно: кинуть асинк запрос на проверку токена через время жизни
      Вопрос: соит ли продлевать "токены", т.е. если пользователь имел корректный токен, 
      но он истекает, то заного послать форму и перезаписать токен 

      }

 [-] write function from onClick(LoginPage) as separate function


 [-] Карточка контракта:
   всего - три строки: 
      - Наименование(одно на строке), 
      - Середина(может на несколько строк расползтись): 
         - номер - цена: равное маленькое расстояние 
         - название, ген подрядчик: побольше расстояние
      - Примечание(одно на строке)


 [?] pseodoclasses:
    полезная штука, например, чтоб при наведении на контракт, он подсвечивался 

  


 ?? --------------- ?? 
   0) Почему группирование условия вызавает ошибку?
       date !== null ? Date = transformDateFormat(date.date) : Date = ' — '
          - всё ОК
       (date !== null) ? Date = transformDateFormat(date.date) : Date = ' — '
          - Uncaught (in promise) TypeError: " \u2014 " is not a function
       - почему теперь ожидается, что в Date должна пойти функция, а не строка, например?  

   1) Вопрос: как сделать чисто?
      - Появилась проблема с доступом к стейту вне реакт компонента(хуки не дают использовать там)
        Суть в том, что при нажатии кнопки "войти" state дополняется нужной для авторизации информацией
        поэтому sate надо получить заного, но из функции-обработчика нажатия нельзя использовать хуки
  
        единственное, что я нашёл - экспортировать store (созданный configureStore),
        но мне что-то подсказывает, что это не очень чистая практика)))
        что делать?
      
   2) Залогиненность - это кто?
      определить, что произошёл логин <=> 
      
         ввести состояние(булево) залогиненности - проблема, тк токены имеют время жизни
         Правильно: кинуть асинк запрос на проверку токена через время жизни, наверно
         
         Вопрос: соит ли продлевать "токены", т.е. если пользователь имел корректный токен, 
         но он истекает, то заного послать форму и перезаписать токен 
         - может, это вообще есть в доке?
      